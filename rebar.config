{erl_opts, [warnings_as_errors, debug_info]}.

{lib_dirs, [".."]}.

{port_envs, [
    {"DRV_LDFLAGS", "$DRV_LDFLAGS -luuid"},
    {"DRV_CFLAGS",  "$DRV_CFLAGS -pedantic -O2"}
]}.

{so_specs, [
    {"priv/uuid.so", ["c_src/uuid.o"]}
]}.

{clean_files, ["doc"]}.
